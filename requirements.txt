# Third-party libraries used by the *_demo.py scripts.
# Standard-library demos (json, os, subprocess, logging, argparse,
# datetime, collections, concurrency) need nothing installed.
#
# Install all with:  pip install -r requirements.txt
pandas>=2.0
pytest>=8.0
pyyaml>=6.0
kubernetes>=29.0
boto3>=1.34
requests>=2.31
tenacity>=8.0
prometheus_client>=0.20
